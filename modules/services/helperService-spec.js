describe('helperService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(helperService) {

    //expect(helperService.doSomething()).toEqual('something');

  }));

});
