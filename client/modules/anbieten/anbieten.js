angular.module('app').controller('AnbietenCtrl',function($scope){

    // Check if is authenticated
        // if not -> show signin content

            // if want to register -> show signup content

        // if is -> show anbieten content

});
